export const environment = {
	production: false,
	apiUrl: "http://localhost:3000/api", // Backend API base URL (fixed - removed /wordle)
	hubUrl: "http://localhost:4200", // PexNet Hub URL for PostMessage validation
};
